apiVersion: v1
kind: Service
metadata:
  name: flask-calculator
spec:
  type: NodePort
  selector:
    app: flask-calculator
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 32093   # optional (30000-32767)
